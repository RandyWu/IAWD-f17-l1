starting_temp = int(raw_input("What is the degree celcius you want to convert?: "))

converted_temp = (starting_temp * 1.8) + 32

print ("{} celcius is also {} fahrenheit".format(starting_temp, converted_temp))
